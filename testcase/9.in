21
10
1 2
3 4
5 6
7 8
9 0
1 0
3 2
5 4
7 6
9 8
#include<iostream>
#include<cstdio>
using namespace std;

int A, B, a0, b0;

int init()
{
  A = B = 0;
}

int a()
{
  A = a0;
  B = b0;
}

int b()
{
  A = A + (B = B + 233);
}

int c()
{
  B = B * A;
}

int endproc()
{
  cout << A;
  putchar(32);
  cout << B << endl;
}

int onecase()
{
  cin >> a0 >> b0;
  init(); a(); b(); c(); endproc();
  init(); a(); c(); b(); endproc();
  init(); b(); a(); c(); endproc();
  init(); b(); c(); a(); endproc();
  init(); c(); a(); b(); endproc();
  init(); c(); b(); a(); endproc();
}

int N;

int main()
{
  for (cin >> N; N; N = N - 1) onecase();
}

